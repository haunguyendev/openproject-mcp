# Metadata cho công cụ phát triển (lint/format).
# Server chạy bằng `uv run --script` với dependency khai báo PEP 723 trong server/server.py.

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
